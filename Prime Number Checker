#include<stdio.h>
int main()
{
  int num,i,count=0;
  scanf("%d",&num);
  for(int i = 2; i<num ; i++)
   {
     if(num %i == 0)
       count++;
       break;
      }
     if(count==0)
        printf("Prime Number\n");
     else
        printf("Not Prime Number");

     return 0;

     }



